# FielCup — dependências do projeto
# Instale com: pip install -r requirements.txt
#
# (sqlite3 já vem na biblioteca padrão do Python — nenhuma dependência extra
#  é necessária para a camada SQL.)

pandas>=2.0
numpy>=1.24
scipy>=1.10
streamlit>=1.58

# Opcional: só para a coleta automática de resultados via API-Football
# (o registro manual de placares no dashboard funciona sem isto)
requests>=2.31
